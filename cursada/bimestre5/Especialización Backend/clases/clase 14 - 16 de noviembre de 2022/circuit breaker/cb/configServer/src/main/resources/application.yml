server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jonathanvictoricadh/esp-back-1-202210-integrador
          default-label: circuitBreak
          force-pull: true
          search-paths: configData
